package classes.singleton;

/*

this also fulfills the requirements because enums has private constructor
only one instance of objects are present per jvm

 */
public enum FEnumDBConnection {
    INSTANCE
}
